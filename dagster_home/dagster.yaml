storage:
  filesystem:
    base_dir: /opt/dagster/dagster_home/storage

run_launcher:
  module: dagster._core.launcher.default_run_launcher
  class: DefaultRunLauncher

code_servers:
  - module_name: pipelines.main_pipeline
    port: 4000